server:
  port: 80
spring:
  datasource:
    url: jdbc:mysql:///autoclock?characterEncoding=utf8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true
    username: root
    password: xust2020
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    encoding: UTF-8 #编码
    suffix: .html #模板后缀
    mode: HTML #模板
mybatis:
  mapper-locations: classpath:mapper/*.xml